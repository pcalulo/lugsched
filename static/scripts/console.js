
/* 
 * This should be the first file to be loaded in all pages that
 * require JavaScript
 * */

if ((typeof console) == 'undefined') {
    console = {};
    console.log = console.error = function() {};
}

